# SOLOMONAR — dependențe runtime (pinuite soft; CI instalează + `pip install -e packages/solomonar_core`)
# Moștenit din cdep-api-poc + adăugiri (DuckDB, PyYAML).

pydantic>=2.6
python-dateutil>=2.9
pyyaml>=6.0
requests>=2.31
truststore>=0.9          # cert store OS (fix antivirus/firewall MITM pe Windows)
parsel>=1.9              # CSS/XPath
duckdb>=0.10             # engine analitic embedded (staging + gold + graf)
pdfplumber>=0.11         # declarații PDF (Faza 2)
pymupdf>=1.24            # randare PDF→imagine pentru OCR (fără binar de sistem)
rapidocr-onnxruntime>=1.3  # OCR declarații scanate (ONNX, română/latină, fără admin/Tesseract)

# Opțional, per fază (decomentează când e nevoie):
# playwright>=1.42       # ANI SPA (Faza 2) — apoi: playwright install chromium
openpyxl>=3.1
xlrd>=2.0
python-calamine>=0.2
splink>=4.0  # optional: rezolutie probabilistica (build_splink_resolution)
python-igraph>=0.11  # analiză de graf (build_network: inele de control, huburi, firme-pod)
deepdiff>=7.0        # changelog între rebuild-uri (build_changelog)
shapely>=2.0         # simplificare GeoJSON județe (one-time, build geo)
# cdlib>=0.4         # optional: comunități suprapuse (faza 2 — firme-pod fuzzy)
