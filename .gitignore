# Bronze (artefacte brute cache-uite) și build DB — NU se comit
data/raw/
data/build/
*.duckdb
*.duckdb.wal

# SQLite registre canonice: SE COMIT (romega_id stabil) — vezi excepția
!data/build/registry.sqlite

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.env
.env.*
*.egg-info/
dist/
build/

# Playwright / OCR temp
.playwright/
tmp_discovery/
*.tmp

# Intermediari harvest la scară (checkpoint crawl + progres JSONL resume) — NU se comit
data/v1/declaratii/_*.json
data/v1/declaratii/_*.jsonl
# EXCEPȚIE: boilerplate-ul de interese e asset de referință (mic) necesar parserului — SE comite
!data/v1/declaratii/_interese_boilerplate.json

# Pagefind index generat
web/pagefind/

# OS / IDE
.DS_Store
Thumbs.db
.idea/
.vscode/

# Memorie locală (per CLAUDE.md — nu se comite)
.claude/memory/

data/v1/companii/_inventar_*

data/v1/companii/_cv_*

# Output sensibil - DOAR local (decizie user 2026-06-10: cross-ref DNA nepublicat)
_local/
data/v1/declaratii/_*
